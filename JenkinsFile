pipeline {
	agent any
	
	stages {
		stage ('Compilação') {
			steps {
				withMaven(maven: 'MAVEN_3_5_4') {
					bat 'mvn clean install'
				}
			}
		}
		
		stage ('Deploy') {
			when {
				stage 'Compilação'
			}
			steps {
				echo 'Copiando arquivo'
			}
		}
	}

}